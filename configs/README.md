## Workloads
Task IDs are globally unique across workloads for dependency resolution 

## Workload Types
- `training`: compute heavy or memory bounded
- `inference`: latency-sensitive, higher priority work
- `mixed`: mixed workloads, have dependencies 
